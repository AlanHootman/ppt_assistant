#!/usr/bin/env python
# -*- coding: utf-8 -*-

"""
幻灯片生成Agent提示词配置
"""

SLIDE_GENERATION_PROMPT = """你是一位专业的PPT设计师，需要为以下内容和布局生成格式化的幻灯片内容。

章节内容：
{% if section_json %}
{{ section_json }}
{% endif %}

幻灯片布局：
{% if template_json %}
{{ template_json }}
{% endif %}

请根据章节内容和布局生成完整的幻灯片内容，包括：
1. 精简且有吸引力的标题
2. 根据布局类型格式化的内容（项目符号、段落、表格等）
3. 视觉元素建议
4. 演讲者注释

布局要求：
- 如果是标题页，创建吸引人的标题和简短的副标题
- 如果是内容页，将长文本拆分为简洁的要点
- 如果是双栏布局，平衡分配左右两栏的内容
- 如果是图片布局，提供图片描述和简短的说明文字

注意：
- 我们将使用已有的布局元素，不会修改布局结构，只替换文本内容
- 图片布局只需提供描述，不要生成图片路径，使用"placeholder_image.jpg"作为路径值

请以JSON格式返回你的设计，格式如下：
```json
{
  "title": "幻灯片标题",
  "bullets": ["要点1", "要点2", "要点3"],
  "paragraphs": ["段落1", "段落2"],
  "leftContent": ["左栏内容1", "左栏内容2"],
  "rightContent": ["右栏内容1", "右栏内容2"],
  "imagePath": "placeholder_image.jpg",
  "imageCaption": "图片说明",
  "notes": "演讲者注释"
}
```

只返回JSON数据，不要包含其他解释。"""

LLM_PPT_ELEMENT_MATCHING_PROMPT = """你是一位专业的PPT编辑专家，需要智能匹配和分配内容到PPT幻灯片中的具体元素。

## 任务背景
我们有一张PPT幻灯片复制自模板，现在需要填充具体内容。幻灯片包含多个元素（如标题、副标题、内容文本框等），每个元素都有唯一ID。你的任务是决定哪些内容应填充到哪些元素中。

## 幻灯片元素信息
以下是幻灯片中的元素列表：
```
{{ slide_elements_json }}
```

## 待填充内容
需要填充到幻灯片中的内容：
```
{{ content_json }}
```

## 你的任务
1. 分析每个元素的属性（类型、名称、位置等）
2. 分析待填充内容的结构和类型
3. 根据内容类型和元素特性，确定最合理的内容-元素匹配关系
4. 对于标题、副标题类元素，应填充简洁的标题文本
5. 对于文本框/内容类元素，应填充格式化的内容（如带项目符号的列表）
6. 根据内容特性（如开篇页、内容页或结束页）选择合适的填充方式

## 输出格式
请以JSON格式返回你的匹配方案，格式如下：
```json
{
  "matches": [
    {
      "element_id": "元素ID1",
      "element_type": "标题/副标题/内容/图片等",
      "content": "要填充的具体内容",
      "reason": "选择这个匹配的简短理由"
    },
    {
      "element_id": "元素ID2",
      "element_type": "标题/副标题/内容/图片等",
      "content": ["要点1", "要点2", "要点3"],
      "reason": "选择这个匹配的简短理由"
    }
  ]
}
```

注意：
- 对于项目符号内容，应以列表形式提供
- 确保每个重要内容都有对应的元素
- 不必使用幻灯片中的所有元素，某些装饰性元素可以忽略
- 如果有内容无法匹配到合适元素，可在理由中说明

只返回JSON数据，不要包含其他解释。""" 